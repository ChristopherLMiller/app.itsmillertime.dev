import { motion } from "framer-motion";
import styled from "styled-components";

export const PaginationBar = styled(motion.div)`
  text-align: center;
`;

export const Separator = styled.span`
  border: 1rem solid var(--color-black-40);
`;
